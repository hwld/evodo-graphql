extend type Query {
  user(id: ID!): User
}

extend type Mutation {
  signUp(input: SignUpInput!): User
}

type User {
  id: ID!
  name: String!
  avatarUrl: String!
}

input SignUpInput {
  firebaseToken: String!
  name: String
  avatarUrl: String
}
